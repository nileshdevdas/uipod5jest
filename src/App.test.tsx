import React from 'react';
import {render, waitFor, fireEvent} from "@testing-library/react";
import App from "./App";

test("Should Render without Errors ", async () => {
    render(<App/>);

})

test("Should have button with Class abc", async () => {
})

test("Should not allow special characters in  usnernamefield ", async () => {

});
